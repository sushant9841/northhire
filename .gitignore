# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files - none exist yet (no real secrets in this app), excluded preemptively
# so a future real API key/credential never lands in git by accident.
.env
.env.*
!.env.example

# Local backend database (regenerate with `npm run server:seed`)
server/data/*.sqlite
server/data/*.sqlite-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
